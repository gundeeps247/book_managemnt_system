#!/bin/sh

# Create the SQLite database and any initial setup here
sqlite3 books.db

# Add any necessary SQL commands to set up the database schema or initial data
# Example: sqlite3 books.db "CREATE TABLE IF NOT EXISTS books (id INTEGER PRIMARY KEY AUTOINCREMENT, title TEXT);"
